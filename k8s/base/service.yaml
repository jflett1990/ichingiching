apiVersion: v1
kind: Service
metadata:
  name: wisdom-oracle-service
  labels:
    app: wisdom-oracle
    component: frontend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: wisdom-oracle
    component: frontend